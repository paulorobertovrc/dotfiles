# ctx-dash Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Transformar o `ctx-watch.sh` (painel de terminal) em `ctx-lib.js` + CLI + um dashboard web local sob demanda que monitora sessões Claude, subagents, jobs em background e workflows.

**Architecture:** Extrair o node embutido em string do `ctx-watch.sh` para módulos reais. `ctx-lib.js` concentra descoberta, leitura de transcript e métricas derivadas, com `home` e `now` **injetáveis** (permite fixtures e clock congelado nos testes). `ctx-watch.js` mantém a apresentação de terminal atual. `ctx-dash.js` serve HTTP em `127.0.0.1` com SSE, e `ctx-dash.html` é uma página self-contained (CSS/JS/SVG inline, sem CDN).

**Tech Stack:** Node.js ≥18 (v24.16 no WSL), zero dependências externas, `node:test` nativo como runner, `node:http`, `fs.watch`, SVG à mão.

**Spec:** `docs/superpowers/specs/2026-07-10-ctx-dash-design.md`

## Global Constraints

- **Zero dependências externas.** Nada de `npm install`, nenhum `package.json` com deps. Só stdlib do node.
- **Sem CDN.** `ctx-dash.html` é self-contained: CSS, JS e SVG inline. Deve funcionar offline.
- **Bind exclusivo em `127.0.0.1`.** Nunca `0.0.0.0`. Transcripts contêm código e dados sensíveis.
- **Somente leitura.** Nenhum endpoint escreve em disco nem comanda sessões. Sem POST/PUT/DELETE.
- **Nunca inventar preço.** A tabela `PRICES` só recebe valores verificados na fonte oficial, com data. Modelo sem preço verificado → `costOf()` retorna `null` e a UI mostra apenas tokens.
- **A saída do terminal não pode regredir.** `ctx-watch` mantém formato, colunas e cores atuais (garantido por teste com fixture + clock congelado).
- **Porta padrão `4791`**, `--port` para override; se ocupada, tenta a próxima e avisa no stdout.
- **Guard anti-reuso de PID obrigatório.** `~/.claude/sessions/<pid>.json` tem 7 órfãos em 14 arquivos (verificado 2026-07-10). Validar `procStart` contra `/proc/<pid>/stat` campo 22 antes de considerar uma sessão viva.
- **Comentários e mensagens de commit em português**, seguindo o padrão do repo (Conventional Commits + linha de co-author).

---

## File Structure

| Arquivo | Responsabilidade |
| --- | --- |
| `claude/ctx-lib.js` | Núcleo: descoberta de sessões, leitura de transcript, janela inferida, métricas, timeline, subagents, jobs, custo. Sem I/O de apresentação. |
| `claude/ctx-watch.js` | CLI: formatação de terminal (barra, cores, idade), modos `--watch`/`--all`. Consome a lib. |
| `claude/ctx-watch.sh` | Wrapper fino: `exec node "$(dirname "$0")/ctx-watch.js" "$@"`. |
| `claude/ctx-dash.js` | Servidor HTTP sob demanda + SSE. Consome a lib. |
| `claude/ctx-dash.html` | Página única: grid de cards + drill-down. |
| `claude/test/*.test.js` | Testes com `node --test`. |
| `claude/test/fixtures/` | HOME falso com transcripts e sessions sintéticos. |
| `install.sh` | Copiar os novos arquivos. |

**Ordem das tarefas:** 1 (extração + testes) é pré-requisito de todas. 2–5 constroem a lib incrementalmente. 6 serve os dados. 7–8 desenham. 9 fecha.

---

### Task 1: Extrair `ctx-lib.js` + `ctx-watch.js` sem regredir o terminal

Fundação. O node embutido em string vira módulos, com `home`/`now` injetáveis para testabilidade.

**Files:**
- Create: `claude/ctx-lib.js`
- Create: `claude/ctx-watch.js`
- Create: `claude/test/fixtures/home/.claude/projects/-fake-proj/aaaaaaaa-0000-0000-0000-000000000001.jsonl`
- Create: `claude/test/render.test.js`
- Modify: `claude/ctx-watch.sh` (substituir o corpo inteiro pelo wrapper)

**Interfaces:**
- Consumes: nada.
- Produces:
  - `ctx-lib.js` exporta: `roots(home)`, `indexAll(home)`, `readInfo(file)`, `windowFor(file, ctx)`, `tierFor(peak)`, `scanWindow(file)`, `whereName(cwd)`, `liveProcs()`, `TIERS`.
  - `readInfo(file)` → `{ctx: number, model: string, cwd: string, ts: string} | null`
  - `indexAll(home)` → `Map<string, {id, file, projectDir, mtime}>`
  - `ctx-watch.js` exporta `render(opts)` onde `opts = {home, now, all, watch, effortLevel, procs}` → `string`. `procs` (opcional) substitui `liveProcs()` — existe para tornar os testes determinísticos.

- [ ] **Step 1: Criar a fixture de transcript**

Criar `claude/test/fixtures/home/.claude/projects/-fake-proj/aaaaaaaa-0000-0000-0000-000000000001.jsonl` com exatamente estas 2 linhas (uma por linha, sem quebra dentro do JSON):

```
{"type":"user","cwd":"/fake/proj","timestamp":"2026-01-01T00:00:00.000Z","gitBranch":"main","permissionMode":"auto","version":"2.1.205","message":{"role":"user","content":"oi"}}
{"type":"assistant","cwd":"/fake/proj","timestamp":"2026-01-01T00:01:00.000Z","permissionMode":"auto","message":{"role":"assistant","model":"claude-opus-4-8","usage":{"input_tokens":10,"output_tokens":100,"cache_read_input_tokens":49990,"cache_creation_input_tokens":0}}}
```

Isso dá `ctx = 10 + 49990 + 0 = 50000`, janela inferida `200000`, portanto `25%`.

- [ ] **Step 2: Escrever o teste que falha**

Criar `claude/test/render.test.js`:

```js
const test = require("node:test");
const assert = require("node:assert");
const path = require("node:path");
const { render } = require("../ctx-watch.js");

const HOME = path.join(__dirname, "fixtures", "home");
const NOW = Date.parse("2026-01-01T00:01:30.000Z");  // 30s após o último turno → "agora"

// procs: [] torna o teste determinístico — sem ele, render() chamaria
// liveProcs() e leria os processos reais da máquina.
const BASE = { home: HOME, now: NOW, watch: false, effortLevel: "", procs: [] };

test("render --all mostra a sessão da fixture com 25% de contexto", () => {
  const out = render({ ...BASE, all: true });
  assert.match(out, /aaaaaaaa/);          // id curto
  assert.match(out, /opus-4-8/);          // modelo sem o prefixo claude-
  // separador de milhar depende do locale (toLocaleString) — aceitar , ou .
  assert.match(out, /50[.,]000 \/ 200k \(25%\)/);
  assert.match(out, /1 sessões, 0 ativa\(s\)/);
});

test("render sem --all e sem processos vivos retorna vazio", () => {
  const out = render({ ...BASE, all: false });
  assert.match(out, /nenhuma sessão ativa do Claude/);
});
```

- [ ] **Step 3: Rodar e ver falhar**

Run: `node --test claude/test/`
Expected: FAIL — `Cannot find module '../ctx-watch.js'`

- [ ] **Step 4: Criar `claude/ctx-lib.js`**

Copiar **literalmente** as funções do bloco node dentro de `claude/ctx-watch.sh` (linhas 26–160), trocando `HOME` por parâmetro. Nada de reescrever a lógica — é extração:

```js
"use strict";
const fs = require("fs"), path = require("path"), os = require("os"), cp = require("child_process");

const TIERS = [200000, 1000000];

function roots(home = os.homedir()) {
  const r = [], add = p => { try { if (fs.statSync(p).isDirectory()) r.push(p); } catch {} };
  add(path.join(home, ".claude", "projects"));
  for (const d of ["c", "d", "e"]) {
    let us; try { us = fs.readdirSync("/mnt/" + d + "/Users"); } catch { continue; }
    for (const u of us) add("/mnt/" + d + "/Users/" + u + "/.claude/projects");
  }
  return r;
}

function indexAll(home) {
  const idx = new Map();
  for (const root of roots(home)) {
    let projs; try { projs = fs.readdirSync(root); } catch { continue; }
    for (const proj of projs) {
      const pdir = path.join(root, proj);
      let files; try { files = fs.readdirSync(pdir); } catch { continue; }
      for (const f of files) {
        if (!f.endsWith(".jsonl")) continue;
        const full = path.join(pdir, f);
        let m = 0; try { m = fs.statSync(full).mtimeMs; } catch {}
        idx.set(f.slice(0, -6), { id: f.slice(0, -6), file: full, projectDir: pdir, mtime: m });
      }
    }
  }
  return idx;
}

function fullArgs(pid) {
  try { return fs.readFileSync("/proc/" + pid + "/cmdline", "utf8").replace(/\0/g, " ").trim(); } catch {}
  try { return cp.execSync("ps -ww -p " + pid + " -o args=", { encoding: "utf8" }).trim(); } catch {}
  return "";
}
function procCwd(pid) {
  try { return fs.readlinkSync("/proc/" + pid + "/cwd"); } catch {}
  try {
    const o = cp.execSync("lsof -a -p " + pid + " -d cwd -Fn 2>/dev/null", { encoding: "utf8" });
    const l = o.split("\n").find(x => x[0] === "n"); if (l) return l.slice(1);
  } catch {}
  return "";
}
function liveProcs() {
  let out = ""; try { out = cp.execSync("ps -eo pid=,comm=", { encoding: "utf8" }); } catch { return []; }
  const pids = [];
  for (const line of out.split("\n")) {
    const m = line.match(/^\s*(\d+)\s+(.+?)\s*$/); if (!m) continue;
    if (path.basename(m[2]) === "claude") pids.push(m[1]);
  }
  return pids.map(pid => {
    const a = fullArgs(pid);
    const rm = a.match(/--resume[\s=]+([0-9a-fA-F-]{36})/);
    return { pid, resume: rm ? rm[1] : "", cwd: procCwd(pid) };
  });
}

function tailRead(full, bytes = 524288) {
  const fd = fs.openSync(full, "r");
  try {
    const size = fs.fstatSync(fd).size, start = Math.max(0, size - bytes);
    const buf = Buffer.alloc(size - start); fs.readSync(fd, buf, 0, buf.length, start);
    let s = buf.toString("utf8"); if (start > 0) s = s.slice(s.indexOf("\n") + 1); return s;
  } finally { fs.closeSync(fd); }
}

function readInfo(file) {
  let lines; try { lines = tailRead(file).split("\n").filter(Boolean); } catch { return null; }
  for (let i = lines.length - 1; i >= 0; i--) {
    let o; try { o = JSON.parse(lines[i]); } catch { continue; }
    const u = o.message && o.message.usage;
    if (u && u.input_tokens != null) {
      return {
        ctx: (u.input_tokens || 0) + (u.cache_read_input_tokens || 0) + (u.cache_creation_input_tokens || 0),
        model: (o.message.model || "").replace("claude-", ""),
        cwd: o.cwd || "", ts: o.timestamp || ""
      };
    }
  }
  return null;
}

function whereName(cwd) {
  if (!cwd) return "—";
  try {
    const top = cp.execSync("git -C " + JSON.stringify(cwd) + " rev-parse --show-toplevel 2>/dev/null",
      { encoding: "utf8" }).trim();
    if (top) return path.basename(top);
  } catch {}
  return path.basename(cwd) || cwd;
}

const winCache = new Map();
function tierFor(peak) {
  for (const t of TIERS) if (peak <= t) return t;
  return Math.ceil(peak / 200000) * 200000;
}
function scanWindow(file) {
  let peak = 0, txt;
  try { txt = fs.readFileSync(file, "utf8"); } catch { return TIERS[0]; }
  for (const line of txt.split("\n")) {
    if (!line || (line.indexOf('"usage"') < 0 && line.indexOf('"preTokens"') < 0)) continue;
    let o; try { o = JSON.parse(line); } catch { continue; }
    const u = o.message && o.message.usage;
    if (u && u.input_tokens != null) {
      const t = (u.input_tokens || 0) + (u.cache_read_input_tokens || 0) + (u.cache_creation_input_tokens || 0);
      if (t > peak) peak = t;
    }
    if (o.compactMetadata && o.compactMetadata.preTokens > peak) peak = o.compactMetadata.preTokens;
    if (peak > TIERS[0]) return TIERS[TIERS.length - 1];
  }
  return tierFor(peak);
}
function windowFor(file, currentCtx) {
  let w = winCache.get(file);
  if (w == null) { w = scanWindow(file); winCache.set(file, w); }
  const cur = tierFor(currentCtx);
  if (cur > w) { w = cur; winCache.set(file, w); }
  return w;
}

module.exports = { TIERS, roots, indexAll, liveProcs, readInfo, whereName, tierFor, scanWindow, windowFor, tailRead };
```

- [ ] **Step 5: Criar `claude/ctx-watch.js`**

Formatação e `render()` migrados do shell, agora com `now` injetável (era `Date.now()`) e `home` injetável:

```js
#!/usr/bin/env node
"use strict";
const fs = require("fs"), path = require("path"), os = require("os");
const lib = require("./ctx-lib.js");

const pct = (c, win) => c / win * 100;
const winLabel = w => (w >= 1e6 ? (w / 1e6) + "M" : Math.round(w / 1000) + "k");
const bar = p => "█".repeat(Math.min(Math.round(p / 5), 20)).padEnd(20, "·");
const col = p => (p >= 80 ? 31 : p >= 50 ? 33 : 32);
const age = (ms, now) => {
  const s = Math.max(0, (now - ms) / 1000);
  if (s < 60) return "agora";
  if (s < 3600) return Math.floor(s / 60) + "m";
  if (s < 86400) return Math.floor(s / 3600) + "h";
  return Math.floor(s / 86400) + "d";
};
const trunc = (s, n) => (s.length > n ? s.slice(0, n - 1) + "…" : s);
const clockOf = now => new Date(now).toTimeString().slice(0, 8);

function readEffort(home) {
  try {
    const cfg = JSON.parse(fs.readFileSync(path.join(home, ".claude", "settings.json"), "utf8"));
    return cfg.effortLevel || "";
  } catch { return ""; }
}

function render(opts = {}) {
  const home = opts.home || os.homedir();
  const now = opts.now || Date.now();
  const ALL = !!opts.all, WATCH = !!opts.watch;
  const effortLevel = opts.effortLevel !== undefined ? opts.effortLevel : readEffort(home);

  const idx = lib.indexAll(home);
  const liveIds = new Set(), liveCwd = new Map();
  for (const p of (opts.procs || lib.liveProcs())) {
    let id = "";
    if (p.resume && idx.has(p.resume)) id = p.resume;
    else if (p.cwd) {
      const enc = p.cwd.replace(/\//g, "-");
      const c = [...idx.values()].filter(r => path.basename(r.projectDir) === enc)
                                 .sort((a, b) => b.mtime - a.mtime)[0];
      if (c) id = c.id;
    }
    if (id) { liveIds.add(id); if (p.cwd) liveCwd.set(id, p.cwd); }
  }

  let pool = ALL ? [...idx.values()] : [...idx.values()].filter(r => liveIds.has(r.id));
  pool.sort((a, b) => b.mtime - a.mtime);

  const rows = [];
  for (const r of pool) {
    const info = lib.readInfo(r.file); if (!info) continue;
    const cwd = liveCwd.get(r.id) || info.cwd || "";
    rows.push({ id: r.id.slice(0, 8), model: info.model, where: lib.whereName(cwd),
                mtime: r.mtime, ctx: info.ctx, win: lib.windowFor(r.file, info.ctx), live: liveIds.has(r.id) });
  }

  if (!rows.length) return (ALL ? "(nenhuma sessão encontrada)" : "(nenhuma sessão ativa do Claude)") + "\n";

  let out = (ALL ? "  " : "") + "sessão    modelo          onde                  últ.ativ.    contexto\n";
  for (const r of rows) {
    const p = pct(r.ctx, r.win), c = col(p);
    const m = ALL ? (r.live ? "\x1b[32m●\x1b[0m " : "  ") : "";
    out += m +
      r.id.padEnd(10) +
      trunc(r.model || "—", 15).padEnd(16) +
      trunc(r.where || "—", 21).padEnd(22) +
      age(r.mtime, now).padEnd(13) +
      "\x1b[" + c + "m" + bar(p) + " " + String(r.ctx.toLocaleString()).padStart(9) +
      " / " + winLabel(r.win) + " (" + p.toFixed(0) + "%)\x1b[0m\n";
  }
  const n = rows.length, liveN = rows.filter(r => r.live).length;
  const stat = ALL ? (n + " sessões, " + liveN + " ativa(s) ●") : (n + " sessão(ões) ativa(s)");
  const efColor = { low: 32, medium: 33, high: 35 }[effortLevel] || 37;
  const efTag = effortLevel ? " · \x1b[" + efColor + "mef:" + effortLevel + "\x1b[2m" : "";
  out += "\x1b[2m" + stat + efTag + " · janela inferida por sessão" +
         (WATCH ? " · ao vivo " + clockOf(now) + " · Ctrl+C p/ sair" : "") + "\x1b[0m\n";
  return out;
}

module.exports = { render };

if (require.main === module) {
  const ALL = process.argv.includes("--all");
  const WATCH = process.argv.includes("--watch") || process.argv.includes("-w");
  if (!WATCH) { process.stdout.write(render({ all: ALL })); }
  else {
    const draw = () => process.stdout.write("\x1b[H" + render({ all: ALL, watch: true }) + "\x1b[0J");
    let timer = null;
    const schedule = () => { if (timer) return; timer = setTimeout(() => { timer = null; draw(); }, 200); };
    const watched = new Set();
    function attach() {
      for (const root of lib.roots()) {
        if (!watched.has(root)) { try { fs.watch(root, schedule); watched.add(root); } catch {} }
        let projs = []; try { projs = fs.readdirSync(root); } catch {}
        for (const proj of projs) {
          const d = path.join(root, proj);
          if (!watched.has(d)) { try { fs.watch(d, schedule); watched.add(d); } catch {} }
        }
      }
    }
    process.stdout.write("\x1b[2J");
    attach(); draw();
    setInterval(() => { attach(); draw(); }, 2000);
  }
}
```

- [ ] **Step 6: Rodar e ver passar**

Run: `node --test claude/test/`
Expected: PASS — 2 testes, 0 falhas.

- [ ] **Step 7: Substituir `ctx-watch.sh` pelo wrapper**

Manter o cabeçalho de documentação (linhas 1–24 do arquivo atual, os comentários `#`) e trocar o corpo por:

```bash
exec node "$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)/ctx-watch.js" "$@"
```

- [ ] **Step 8: Sanity-check contra as sessões reais**

Run: `bash claude/ctx-watch.sh --all | sed -E 's/[0-9]+[mhd]|agora/AGE/g' > /tmp/depois.txt && git stash && bash claude/ctx-watch.sh --all | sed -E 's/[0-9]+[mhd]|agora/AGE/g' > /tmp/antes.txt && git stash pop && diff /tmp/antes.txt /tmp/depois.txt && echo "IDÊNTICO"`

Expected: `IDÊNTICO`. (A normalização da coluna de idade é necessária: ela é volátil entre execuções.) Se houver diff em tokens/%, é porque uma sessão gravou um turno entre as duas execuções — repetir.

- [ ] **Step 9: Commit**

```bash
git add claude/ctx-lib.js claude/ctx-watch.js claude/ctx-watch.sh claude/test/
git commit -m "refactor(ctx): extrai ctx-lib.js e ctx-watch.js do shell, com testes

home/now injetáveis permitem fixtures e clock congelado. Saída do
terminal inalterada (verificada por diff normalizado).

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

### Task 2: Descoberta de sessões via `~/.claude/sessions/<pid>.json`

Substituir a heurística `ps` + mtime pelo registro oficial que o Claude Code mantém, com guard anti-reuso de PID.

**Files:**
- Modify: `claude/ctx-lib.js`
- Create: `claude/test/sessions.test.js`
- Create: `claude/test/fixtures/home/.claude/sessions/999999.json`

**Interfaces:**
- Consumes: `lib.liveProcs()` (fallback).
- Produces: `liveSessions(home, opts)` → `Array<{pid, sessionId, cwd, name, version, entrypoint, kind, startedAt, status, waitingFor}>`. `opts.isAlive(pid, procStart)` injetável (default: guard real).

- [ ] **Step 1: Criar a fixture de sessão**

`claude/test/fixtures/home/.claude/sessions/999999.json`:

```json
{"pid":999999,"sessionId":"aaaaaaaa-0000-0000-0000-000000000001","cwd":"/fake/proj","startedAt":1783686893508,"procStart":"41448","version":"2.1.206","peerProtocol":1,"kind":"interactive","entrypoint":"cli","name":"fake-proj-1","nameSource":"derived"}
```

- [ ] **Step 2: Escrever o teste que falha**

`claude/test/sessions.test.js`:

```js
const test = require("node:test");
const assert = require("node:assert");
const path = require("node:path");
const lib = require("../ctx-lib.js");

const HOME = path.join(__dirname, "fixtures", "home");

test("liveSessions lê o registro e aplica o guard de procStart", () => {
  const alive = lib.liveSessions(HOME, { isAlive: (pid, ps) => pid === 999999 && ps === "41448" });
  assert.strictEqual(alive.length, 1);
  assert.strictEqual(alive[0].sessionId, "aaaaaaaa-0000-0000-0000-000000000001");
  assert.strictEqual(alive[0].name, "fake-proj-1");
});

test("liveSessions descarta órfão quando procStart não bate (PID reusado)", () => {
  const alive = lib.liveSessions(HOME, { isAlive: (pid, ps) => ps === "OUTRO" });
  assert.strictEqual(alive.length, 0);
});
```

- [ ] **Step 3: Rodar e ver falhar**

Run: `node --test claude/test/sessions.test.js`
Expected: FAIL — `lib.liveSessions is not a function`

- [ ] **Step 4: Implementar `liveSessions` em `ctx-lib.js`**

```js
// Lê /proc/<pid>/stat campo 22 (starttime). Guard contra reuso de PID:
// ~/.claude/sessions/ acumula órfãos (7 de 14 na inspeção de 2026-07-10).
function procStartOf(pid) {
  try {
    const st = fs.readFileSync("/proc/" + pid + "/stat", "utf8");
    return st.slice(st.lastIndexOf(")") + 2).split(" ")[19];
  } catch { return null; }
}

function defaultIsAlive(pid, procStart) {
  const real = procStartOf(pid);
  if (real == null) return false;
  return procStart == null || String(real) === String(procStart);
}

function liveSessions(home = os.homedir(), opts = {}) {
  const isAlive = opts.isAlive || defaultIsAlive;
  const dir = path.join(home, ".claude", "sessions");
  let files; try { files = fs.readdirSync(dir); } catch { return []; }
  const out = [];
  for (const f of files) {
    if (!f.endsWith(".json")) continue;
    let o; try { o = JSON.parse(fs.readFileSync(path.join(dir, f), "utf8")); } catch { continue; }
    if (!o || !o.pid || !o.sessionId) continue;
    if (!isAlive(o.pid, o.procStart)) continue;
    out.push({
      pid: o.pid, sessionId: o.sessionId, cwd: o.cwd || "", name: o.name || "",
      version: o.version || "", entrypoint: o.entrypoint || "", kind: o.kind || "",
      startedAt: o.startedAt || 0,
      // status/waitingFor são gravados de forma esporádica (1 de 14 arquivos em
      // 2026-07-10) — tratar como enriquecimento best-effort, nunca como base.
      status: o.status || null, waitingFor: o.waitingFor || null
    });
  }
  return out;
}
```

Adicionar `liveSessions` e `procStartOf` ao `module.exports`.

- [ ] **Step 5: Rodar e ver passar**

Run: `node --test claude/test/`
Expected: PASS — 4 testes.

- [ ] **Step 6: Validar contra a máquina real**

Run: `node -e 'const l=require("./claude/ctx-lib.js"); const s=l.liveSessions(); console.log(s.length, "vivas"); console.log(s.map(x=>x.pid+" "+x.sessionId.slice(0,8)+" "+x.name).join("\n"))'`
Expected: o número de sessões vivas bate com `ps -eo comm= | grep -c '^claude$'`, e nenhum órfão aparece.

- [ ] **Step 7: Commit**

```bash
git add claude/ctx-lib.js claude/test/
git commit -m "feat(ctx): descoberta de sessões via ~/.claude/sessions com guard de procStart

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

### Task 3: Métricas derivadas por sessão (varredura única, cache por mtime)

**Files:**
- Modify: `claude/ctx-lib.js`
- Create: `claude/test/metrics.test.js`

**Interfaces:**
- Consumes: `indexAll`, `windowFor`.
- Produces: `sessionMetrics(file)` → objeto abaixo. Cacheado por `mtimeMs`.

```js
{
  ctx: number, window: number, pctFull: number,
  model: string,                       // modelo do último turno, sem prefixo "claude-"
  turns: number,                       // linhas type==="assistant"
  totals: { input, output, cacheRead, cacheCreation },
  cacheHitPct: number | null,          // cacheRead / (input+cacheRead+cacheCreation) * 100
  permissionMode: string | null,       // último valor visto
  modeTransitions: Array<{at: string, from: string, to: string}>,
  gitBranch: string | null,
  aiTitle: string | null,
  version: string | null,
  cwd: string,
  lastTs: string,
  speed: string | null, serviceTier: string | null,
  webSearches: number, webFetches: number,
  tools: Record<string, number>,       // contagem de tool_use por nome
  compacts: Array<{at: string, preTokens: number}>,
  byModel: Record<string, {input, output, cacheRead, cacheCreation}>  // custo por-modelo (sessão troca de modelo)
}
```

- [ ] **Step 1: Escrever o teste que falha**

`claude/test/metrics.test.js`:

```js
const test = require("node:test");
const assert = require("node:assert");
const path = require("node:path");
const lib = require("../ctx-lib.js");

const FILE = path.join(__dirname, "fixtures", "home", ".claude", "projects",
  "-fake-proj", "aaaaaaaa-0000-0000-0000-000000000001.jsonl");

test("sessionMetrics deriva contexto, turnos, cache hit e modo", () => {
  const m = lib.sessionMetrics(FILE);
  assert.strictEqual(m.ctx, 50000);
  assert.strictEqual(m.window, 200000);
  assert.strictEqual(m.turns, 1);
  assert.strictEqual(m.model, "opus-4-8");
  assert.strictEqual(m.permissionMode, "auto");
  assert.strictEqual(m.gitBranch, "main");
  assert.strictEqual(m.totals.output, 100);
  // cacheRead 49990 / (10 + 49990 + 0) = 99.98%
  assert.ok(Math.abs(m.cacheHitPct - 99.98) < 0.01);
  assert.deepStrictEqual(m.byModel["claude-opus-4-8"].output, 100);
});
```

- [ ] **Step 2: Rodar e ver falhar**

Run: `node --test claude/test/metrics.test.js`
Expected: FAIL — `lib.sessionMetrics is not a function`

- [ ] **Step 3: Implementar `sessionMetrics`**

Em `ctx-lib.js`:

```js
const metricsCache = new Map();  // file -> {mtime, value}

function sessionMetrics(file) {
  let mtime = 0; try { mtime = fs.statSync(file).mtimeMs; } catch { return null; }
  const hit = metricsCache.get(file);
  if (hit && hit.mtime === mtime) return hit.value;

  const m = {
    ctx: 0, window: TIERS[0], pctFull: 0, model: "", turns: 0,
    totals: { input: 0, output: 0, cacheRead: 0, cacheCreation: 0 },
    cacheHitPct: null, permissionMode: null, modeTransitions: [],
    gitBranch: null, aiTitle: null, version: null, cwd: "", lastTs: "",
    speed: null, serviceTier: null, webSearches: 0, webFetches: 0,
    tools: {}, compacts: [], byModel: {}
  };
  let txt; try { txt = fs.readFileSync(file, "utf8"); } catch { return null; }
  let peak = 0, prevMode = null;

  for (const line of txt.split("\n")) {
    if (!line) continue;
    let o; try { o = JSON.parse(line); } catch { continue; }

    if (o.gitBranch) m.gitBranch = o.gitBranch;
    if (o.aiTitle) m.aiTitle = o.aiTitle;
    if (o.version) m.version = o.version;
    if (o.cwd) m.cwd = o.cwd;
    if (o.timestamp) m.lastTs = o.timestamp;

    if (o.permissionMode) {
      if (prevMode && prevMode !== o.permissionMode) {
        m.modeTransitions.push({ at: o.timestamp || "", from: prevMode, to: o.permissionMode });
      }
      prevMode = o.permissionMode;
      m.permissionMode = o.permissionMode;
    }

    if (o.compactMetadata && o.compactMetadata.preTokens) {
      m.compacts.push({ at: o.timestamp || "", preTokens: o.compactMetadata.preTokens });
      if (o.compactMetadata.preTokens > peak) peak = o.compactMetadata.preTokens;
    }

    if (o.type === "assistant") m.turns++;

    const msg = o.message;
    if (msg && Array.isArray(msg.content)) {
      for (const c of msg.content) if (c && c.type === "tool_use") m.tools[c.name] = (m.tools[c.name] || 0) + 1;
    }

    const u = msg && msg.usage;
    if (u && u.input_tokens != null) {
      const inp = u.input_tokens || 0, cr = u.cache_read_input_tokens || 0, cc = u.cache_creation_input_tokens || 0;
      const total = inp + cr + cc;
      if (total > peak) peak = total;
      m.ctx = total;                       // último turno vence
      m.model = (msg.model || "").replace("claude-", "");
      m.totals.input += inp; m.totals.output += u.output_tokens || 0;
      m.totals.cacheRead += cr; m.totals.cacheCreation += cc;
      if (u.speed) m.speed = u.speed;
      if (u.service_tier) m.serviceTier = u.service_tier;
      if (u.server_tool_use) {
        m.webSearches += u.server_tool_use.web_search_requests || 0;
        m.webFetches += u.server_tool_use.web_fetch_requests || 0;
      }
      const key = msg.model || "unknown";
      const b = m.byModel[key] || (m.byModel[key] = { input: 0, output: 0, cacheRead: 0, cacheCreation: 0 });
      b.input += inp; b.output += u.output_tokens || 0; b.cacheRead += cr; b.cacheCreation += cc;
    }
  }

  m.window = peak > TIERS[0] ? TIERS[TIERS.length - 1] : tierFor(peak);
  if (tierFor(m.ctx) > m.window) m.window = tierFor(m.ctx);
  m.pctFull = m.window ? m.ctx / m.window * 100 : 0;
  const inTot = m.totals.input + m.totals.cacheRead + m.totals.cacheCreation;
  m.cacheHitPct = inTot > 0 ? m.totals.cacheRead / inTot * 100 : null;

  metricsCache.set(file, { mtime, value: m });
  return m;
}
```

Adicionar `sessionMetrics` ao `module.exports`.

- [ ] **Step 4: Rodar e ver passar**

Run: `node --test claude/test/`
Expected: PASS — 5 testes.

- [ ] **Step 5: Commit**

```bash
git add claude/ctx-lib.js claude/test/metrics.test.js
git commit -m "feat(ctx): sessionMetrics com cache por mtime (cache hit, turnos, modos, custo por-modelo)

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

### Task 4: Timeline, custo e tendência

**Files:**
- Modify: `claude/ctx-lib.js`
- Create: `claude/test/timeline.test.js`

**Interfaces:**
- Consumes: `sessionMetrics`.
- Produces:
  - `timeline(file)` → `Array<{ts: number, ctx: number, output: number, model: string, compact: boolean}>` (ordem cronológica), cacheado por mtime.
  - `trend(points, windowMs)` → `{tokensPerMin: number, etaMinutes: number | null}` — `etaMinutes` é `null` se a tendência for ≤0.
  - `PRICES` (objeto) e `costOf(byModel)` → `{usd: number, partial: boolean} | null`.

- [ ] **Step 1: Escrever o teste que falha**

`claude/test/timeline.test.js`:

```js
const test = require("node:test");
const assert = require("node:assert");
const path = require("node:path");
const lib = require("../ctx-lib.js");

const FILE = path.join(__dirname, "fixtures", "home", ".claude", "projects",
  "-fake-proj", "aaaaaaaa-0000-0000-0000-000000000001.jsonl");

test("timeline extrai um ponto por turno de assistant", () => {
  const pts = lib.timeline(FILE);
  assert.strictEqual(pts.length, 1);
  assert.strictEqual(pts[0].ctx, 50000);
  assert.strictEqual(pts[0].compact, false);
});

test("trend calcula tokens/min e ETA", () => {
  const t0 = Date.parse("2026-01-01T00:00:00Z");
  const pts = [
    { ts: t0, ctx: 0 },
    { ts: t0 + 60000, ctx: 10000 }   // +10k em 1 min
  ];
  const r = lib.trend(pts, 200000);
  assert.strictEqual(Math.round(r.tokensPerMin), 10000);
  assert.strictEqual(Math.round(r.etaMinutes), 19);   // (200000-10000)/10000
});

test("trend com contexto estável não projeta ETA", () => {
  const t0 = Date.parse("2026-01-01T00:00:00Z");
  const r = lib.trend([{ ts: t0, ctx: 5000 }, { ts: t0 + 60000, ctx: 5000 }], 200000);
  assert.strictEqual(r.etaMinutes, null);
});

test("costOf retorna null para modelo sem preço verificado", () => {
  assert.strictEqual(lib.costOf({ "claude-modelo-inexistente": { input: 1, output: 1, cacheRead: 0, cacheCreation: 0 } }), null);
});
```

- [ ] **Step 2: Rodar e ver falhar**

Run: `node --test claude/test/timeline.test.js`
Expected: FAIL — `lib.timeline is not a function`

- [ ] **Step 3: Buscar os preços na fonte oficial**

**NÃO INVENTAR.** Invocar a skill `claude-api` (ou consultar https://docs.claude.com/en/docs/about-claude/pricing) e extrair, para cada modelo que aparece nos transcripts (`claude-opus-4-8`, `claude-fable-5`, `claude-haiku-4-5-20251001`), os preços por 1M de tokens de: input, output, cache read, cache write (5m). Anotar a data da consulta.

Se um modelo não tiver preço confirmado, **deixá-lo fora da tabela** — `costOf` marcará `partial: true`.

- [ ] **Step 4: Implementar `timeline`, `trend`, `PRICES`, `costOf`**

```js
const timelineCache = new Map();

function timeline(file) {
  let mtime = 0; try { mtime = fs.statSync(file).mtimeMs; } catch { return []; }
  const hit = timelineCache.get(file);
  if (hit && hit.mtime === mtime) return hit.value;

  const pts = [];
  let txt; try { txt = fs.readFileSync(file, "utf8"); } catch { return []; }
  for (const line of txt.split("\n")) {
    if (!line) continue;
    let o; try { o = JSON.parse(line); } catch { continue; }
    if (o.compactMetadata && o.compactMetadata.preTokens) {
      pts.push({ ts: Date.parse(o.timestamp || 0) || 0, ctx: o.compactMetadata.preTokens,
                 output: 0, model: "", compact: true });
      continue;
    }
    const u = o.message && o.message.usage;
    if (!u || u.input_tokens == null) continue;
    pts.push({
      ts: Date.parse(o.timestamp || 0) || 0,
      ctx: (u.input_tokens || 0) + (u.cache_read_input_tokens || 0) + (u.cache_creation_input_tokens || 0),
      output: u.output_tokens || 0,
      model: (o.message.model || "").replace("claude-", ""),
      compact: false
    });
  }
  pts.sort((a, b) => a.ts - b.ts);
  timelineCache.set(file, { mtime, value: pts });
  return pts;
}

// Tendência pela regressão simples entre o primeiro e o último ponto da janela
// recente (últimos 5 pontos não-compact). Compacts quebram monotonicidade.
function trend(points, windowTokens) {
  const p = points.filter(x => !x.compact).slice(-5);
  if (p.length < 2) return { tokensPerMin: 0, etaMinutes: null };
  const a = p[0], b = p[p.length - 1];
  const dtMin = (b.ts - a.ts) / 60000;
  if (dtMin <= 0) return { tokensPerMin: 0, etaMinutes: null };
  const tokensPerMin = (b.ctx - a.ctx) / dtMin;
  if (tokensPerMin <= 0) return { tokensPerMin, etaMinutes: null };
  const remaining = windowTokens - b.ctx;
  return { tokensPerMin, etaMinutes: remaining > 0 ? remaining / tokensPerMin : 0 };
}

// USD por 1.000.000 de tokens. Preencher SOMENTE com valores verificados na
// fonte oficial. Modelo ausente => costOf marca partial e não inventa número.
// Fonte: <URL>  ·  Verificado em: <AAAA-MM-DD>
const PRICES = {
  // "claude-opus-4-8": { input: _, output: _, cacheRead: _, cacheWrite: _ },
};

function costOf(byModel) {
  let usd = 0, partial = false, any = false;
  for (const [model, t] of Object.entries(byModel || {})) {
    const p = PRICES[model];
    if (!p) { partial = true; continue; }
    any = true;
    usd += (t.input / 1e6) * p.input
         + (t.output / 1e6) * p.output
         + (t.cacheRead / 1e6) * p.cacheRead
         + (t.cacheCreation / 1e6) * p.cacheWrite;
  }
  if (!any) return null;
  return { usd, partial };
}
```

Adicionar `timeline`, `trend`, `PRICES`, `costOf` ao `module.exports`.

- [ ] **Step 5: Rodar e ver passar**

Run: `node --test claude/test/`
Expected: PASS — 9 testes.

- [ ] **Step 6: Commit**

```bash
git add claude/ctx-lib.js claude/test/timeline.test.js
git commit -m "feat(ctx): timeline, tendência/ETA e custo (preços só de fonte verificada)

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

### Task 5: Subagents, jobs em background e workflows

**Files:**
- Modify: `claude/ctx-lib.js`
- Create: `claude/test/agents.test.js`
- Create: `claude/test/fixtures/home/.claude/projects/-fake-proj/aaaaaaaa-0000-0000-0000-000000000001/subagents/agent-abc123.jsonl`
- Create: `claude/test/fixtures/home/.claude/projects/-fake-proj/aaaaaaaa-0000-0000-0000-000000000001/subagents/agent-abc123.meta.json`
- Create: `claude/test/fixtures/home/.claude/jobs/job1/state.json`

**Interfaces:**
- Consumes: nada novo.
- Produces:
  - `subagentsFor(file)` → `Array<{agentId, agentType, description, spawnDepth, model, tokens: {input,output}, mtime}>`
  - `backgroundJobs(home)` → `Array<{id, state, sessionId, name, intent, tokens, inFlight, model}>`
  - `workflowsFor(file)` → `Array<object>` — **tolerante**: retorna `[]` se `journal.jsonl` não existir.

- [ ] **Step 1: Criar fixtures**

`.../aaaaaaaa-.../subagents/agent-abc123.meta.json`:

```json
{"agentType":"Explore","description":"Buscar padrões","toolUseId":"toolu_x","spawnDepth":1}
```

`.../aaaaaaaa-.../subagents/agent-abc123.jsonl` (2 linhas):

```
{"isSidechain":true,"agentId":"abc123","type":"user","message":{"role":"user","content":"faça X"}}
{"isSidechain":true,"agentId":"abc123","type":"assistant","message":{"model":"claude-haiku-4-5-20251001","role":"assistant","usage":{"input_tokens":5,"output_tokens":50,"cache_read_input_tokens":1000,"cache_creation_input_tokens":0}}}
```

`claude/test/fixtures/home/.claude/jobs/job1/state.json`:

```json
{"state":"done","tokens":4648,"inFlight":{"tasks":0,"queued":0,"kinds":[]},"name":"job de teste","intent":"testar","sessionId":"aaaaaaaa-0000-0000-0000-000000000001","respawnFlags":["--agent","claude","--model","claude-fable-5[1m]"]}
```

- [ ] **Step 2: Escrever o teste que falha**

`claude/test/agents.test.js`:

```js
const test = require("node:test");
const assert = require("node:assert");
const path = require("node:path");
const lib = require("../ctx-lib.js");

const HOME = path.join(__dirname, "fixtures", "home");
const FILE = path.join(HOME, ".claude", "projects", "-fake-proj",
  "aaaaaaaa-0000-0000-0000-000000000001.jsonl");

test("subagentsFor lê jsonl + meta e soma tokens", () => {
  const a = lib.subagentsFor(FILE);
  assert.strictEqual(a.length, 1);
  assert.strictEqual(a[0].agentId, "abc123");
  assert.strictEqual(a[0].agentType, "Explore");
  assert.strictEqual(a[0].model, "haiku-4-5-20251001");
  assert.strictEqual(a[0].tokens.output, 50);
});

test("backgroundJobs lê state.json e extrai o modelo dos respawnFlags", () => {
  const j = lib.backgroundJobs(HOME);
  assert.strictEqual(j.length, 1);
  assert.strictEqual(j[0].state, "done");
  assert.strictEqual(j[0].sessionId, "aaaaaaaa-0000-0000-0000-000000000001");
  assert.strictEqual(j[0].model, "claude-fable-5[1m]");
});

test("workflowsFor tolera ausência de journal", () => {
  assert.deepStrictEqual(lib.workflowsFor(FILE), []);
});
```

- [ ] **Step 3: Rodar e ver falhar**

Run: `node --test claude/test/agents.test.js`
Expected: FAIL — `lib.subagentsFor is not a function`

- [ ] **Step 4: Implementar**

```js
// O transcript-dir é o diretório irmão com o mesmo nome do .jsonl, sem extensão.
function transcriptDir(file) { return file.replace(/\.jsonl$/, ""); }

function subagentsFor(file) {
  const dir = path.join(transcriptDir(file), "subagents");
  let files; try { files = fs.readdirSync(dir); } catch { return []; }
  const out = [];
  for (const f of files) {
    if (!f.endsWith(".jsonl")) continue;
    const agentId = f.replace(/^agent-/, "").replace(/\.jsonl$/, "");
    let meta = {};
    try { meta = JSON.parse(fs.readFileSync(path.join(dir, "agent-" + agentId + ".meta.json"), "utf8")); } catch {}
    let mtime = 0; try { mtime = fs.statSync(path.join(dir, f)).mtimeMs; } catch {}
    const tokens = { input: 0, output: 0 };
    let model = "";
    let txt; try { txt = fs.readFileSync(path.join(dir, f), "utf8"); } catch { continue; }
    for (const line of txt.split("\n")) {
      if (!line) continue;
      let o; try { o = JSON.parse(line); } catch { continue; }
      const u = o.message && o.message.usage;
      if (u && u.input_tokens != null) {
        tokens.input += (u.input_tokens || 0) + (u.cache_read_input_tokens || 0) + (u.cache_creation_input_tokens || 0);
        tokens.output += u.output_tokens || 0;
        if (o.message.model) model = o.message.model.replace("claude-", "");
      }
    }
    out.push({ agentId, agentType: meta.agentType || "", description: meta.description || "",
               spawnDepth: meta.spawnDepth || 0, model, tokens, mtime });
  }
  return out;
}

function backgroundJobs(home = os.homedir()) {
  const dir = path.join(home, ".claude", "jobs");
  let entries; try { entries = fs.readdirSync(dir); } catch { return []; }
  const out = [];
  for (const id of entries) {
    let s; try { s = JSON.parse(fs.readFileSync(path.join(dir, id, "state.json"), "utf8")); } catch { continue; }
    const flags = s.respawnFlags || [];
    const mi = flags.indexOf("--model");
    out.push({
      id, state: s.state || "", sessionId: s.sessionId || "", name: s.name || "",
      intent: s.intent || "", tokens: s.tokens || 0,
      inFlight: s.inFlight || { tasks: 0, queued: 0 },
      model: mi >= 0 && flags[mi + 1] ? flags[mi + 1] : ""
    });
  }
  return out;
}

// Workflows: o journal.jsonl NÃO foi observado em disco (nenhum workflow rodou
// nesta máquina até 2026-07-10). Descoberta tolerante; o formato deve ser
// validado com um workflow real ANTES de desenhar UI de detalhe.
function workflowsFor(file) {
  const j = path.join(transcriptDir(file), "journal.jsonl");
  let txt; try { txt = fs.readFileSync(j, "utf8"); } catch { return []; }
  const out = [];
  for (const line of txt.split("\n")) {
    if (!line) continue;
    try { out.push(JSON.parse(line)); } catch {}
  }
  return out;
}
```

Adicionar `subagentsFor`, `backgroundJobs`, `workflowsFor`, `transcriptDir` ao `module.exports`.

- [ ] **Step 5: Rodar e ver passar**

Run: `node --test claude/test/`
Expected: PASS — 12 testes.

- [ ] **Step 6: Validar contra dados reais**

Run: `node -e 'const l=require("./claude/ctx-lib.js"); console.log("jobs:", l.backgroundJobs().length); const i=l.indexAll(); for(const r of i.values()){const a=l.subagentsFor(r.file); if(a.length) console.log(r.id.slice(0,8), a.length, "subagents", a.map(x=>x.agentType).join(","));}'`
Expected: lista ao menos o job `c2d519db` e os subagents da sessão atual (incluindo `claude-code-guide`).

- [ ] **Step 7: Commit**

```bash
git add claude/ctx-lib.js claude/test/
git commit -m "feat(ctx): descoberta de subagents, jobs em background e workflows (tolerante)

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

### Task 6: Servidor HTTP + SSE (`ctx-dash.js`)

**Files:**
- Create: `claude/ctx-dash.js`
- Create: `claude/test/server.test.js`

**Interfaces:**
- Consumes: toda a lib.
- Produces:
  - `buildSnapshot(home)` → `{sessions: [...], jobs: [...], generatedAt: number}` — um card por sessão.
  - `createServer(opts)` → instância `http.Server` (não escuta sozinha; o CLI chama `.listen`).
  - Rotas: `GET /` (html), `GET /api/sessions`, `GET /api/session/:id/timeline`, `GET /api/events` (SSE).

- [ ] **Step 1: Escrever o teste que falha**

`claude/test/server.test.js`:

```js
const test = require("node:test");
const assert = require("node:assert");
const path = require("node:path");
const { buildSnapshot, createServer } = require("../ctx-dash.js");

const HOME = path.join(__dirname, "fixtures", "home");

test("buildSnapshot inclui a sessão da fixture com métricas e o job", () => {
  const s = buildSnapshot(HOME, { isAlive: () => true });
  const sess = s.sessions.find(x => x.id.startsWith("aaaaaaaa"));
  assert.ok(sess, "sessão da fixture presente");
  assert.strictEqual(sess.ctx, 50000);
  assert.strictEqual(sess.window, 200000);
  assert.strictEqual(sess.permissionMode, "auto");
  assert.strictEqual(s.jobs.length, 1);
});

test("servidor responde /api/sessions com JSON e recusa métodos de escrita", async () => {
  const srv = createServer({ home: HOME, isAlive: () => true });
  await new Promise(r => srv.listen(0, "127.0.0.1", r));
  const port = srv.address().port;

  const res = await fetch(`http://127.0.0.1:${port}/api/sessions`);
  assert.strictEqual(res.status, 200);
  const body = await res.json();
  assert.ok(Array.isArray(body.sessions));

  const post = await fetch(`http://127.0.0.1:${port}/api/sessions`, { method: "POST" });
  assert.strictEqual(post.status, 405);

  await new Promise(r => srv.close(r));
});
```

- [ ] **Step 2: Rodar e ver falhar**

Run: `node --test claude/test/server.test.js`
Expected: FAIL — `Cannot find module '../ctx-dash.js'`

- [ ] **Step 3: Implementar `ctx-dash.js`**

```js
#!/usr/bin/env node
"use strict";
const http = require("http"), fs = require("fs"), path = require("path"), os = require("os");
const cp = require("child_process");
const lib = require("./ctx-lib.js");

const DEFAULT_PORT = 4791;

function buildSnapshot(home = os.homedir(), opts = {}) {
  const idx = lib.indexAll(home);
  const live = lib.liveSessions(home, opts);
  const liveById = new Map(live.map(s => [s.sessionId, s]));
  const sessions = [];

  for (const r of idx.values()) {
    const m = lib.sessionMetrics(r.file);
    if (!m) continue;
    const l = liveById.get(r.id) || null;
    const pts = lib.timeline(r.file);
    const t = lib.trend(pts, m.window);
    const subs = lib.subagentsFor(r.file);
    sessions.push({
      id: r.id, live: !!l, pid: l ? l.pid : null,
      title: m.aiTitle || (l && l.name) || "", where: lib.whereName(m.cwd), branch: m.gitBranch,
      model: m.model, version: m.version || (l && l.version) || "",
      entrypoint: l ? l.entrypoint : "", status: l ? l.status : null, waitingFor: l ? l.waitingFor : null,
      mtime: r.mtime, lastTs: m.lastTs,
      ctx: m.ctx, window: m.window, pctFull: m.pctFull,
      tokensPerMin: t.tokensPerMin, etaMinutes: t.etaMinutes,
      sparkline: pts.filter(p => !p.compact).slice(-40).map(p => p.ctx),
      turns: m.turns, cacheHitPct: m.cacheHitPct, totals: m.totals,
      cost: lib.costOf(m.byModel),
      permissionMode: m.permissionMode, modeTransitions: m.modeTransitions.length,
      speed: m.speed, serviceTier: m.serviceTier,
      webSearches: m.webSearches, webFetches: m.webFetches,
      tools: m.tools, compacts: m.compacts.length,
      subagents: subs.length, subagentTypes: [...new Set(subs.map(s => s.agentType).filter(Boolean))]
    });
  }
  sessions.sort((a, b) => (b.live - a.live) || (b.mtime - a.mtime));
  return { sessions, jobs: lib.backgroundJobs(home), generatedAt: Date.now() };
}

function sendJson(res, obj, status = 200) {
  const b = Buffer.from(JSON.stringify(obj));
  res.writeHead(status, { "content-type": "application/json; charset=utf-8", "content-length": b.length });
  res.end(b);
}

function createServer(opts = {}) {
  const home = opts.home || os.homedir();
  const clients = new Set();

  const srv = http.createServer((req, res) => {
    if (req.method !== "GET") return sendJson(res, { error: "somente leitura" }, 405);
    const url = new URL(req.url, "http://127.0.0.1");

    if (url.pathname === "/") {
      let html; try { html = fs.readFileSync(path.join(__dirname, "ctx-dash.html")); }
      catch { res.writeHead(500); return res.end("ctx-dash.html ausente"); }
      res.writeHead(200, { "content-type": "text/html; charset=utf-8" });
      return res.end(html);
    }

    if (url.pathname === "/api/sessions") return sendJson(res, buildSnapshot(home, opts));

    const tm = url.pathname.match(/^\/api\/session\/([0-9a-fA-F-]{36})\/timeline$/);
    if (tm) {
      const rec = lib.indexAll(home).get(tm[1]);
      if (!rec) return sendJson(res, { error: "sessão não encontrada" }, 404);
      return sendJson(res, {
        points: lib.timeline(rec.file),
        metrics: lib.sessionMetrics(rec.file),
        subagents: lib.subagentsFor(rec.file),
        workflows: lib.workflowsFor(rec.file)
      });
    }

    if (url.pathname === "/api/events") {
      res.writeHead(200, { "content-type": "text/event-stream", "cache-control": "no-cache", connection: "keep-alive" });
      res.write(": conectado\n\n");
      clients.add(res);
      req.on("close", () => clients.delete(res));
      return;
    }

    res.writeHead(404); res.end("não encontrado");
  });

  // fs.watch nas roots + debounce 200ms (mesmo mecanismo do --watch do terminal)
  let timer = null;
  const notify = () => {
    if (timer) return;
    timer = setTimeout(() => {
      timer = null;
      for (const c of clients) { try { c.write("event: change\ndata: {}\n\n"); } catch {} }
    }, 200);
  };
  const watched = new Set();
  const attach = () => {
    for (const root of lib.roots(home)) {
      if (!watched.has(root)) { try { fs.watch(root, notify); watched.add(root); } catch {} }
      let projs = []; try { projs = fs.readdirSync(root); } catch {}
      for (const p of projs) {
        const d = path.join(root, p);
        if (!watched.has(d)) { try { fs.watch(d, notify); watched.add(d); } catch {} }
      }
    }
  };
  attach();
  const iv = setInterval(attach, 5000);
  srv.on("close", () => clearInterval(iv));
  return srv;
}

function openBrowser(url) {
  for (const cmd of [["wslview", url], ["explorer.exe", url], ["xdg-open", url]]) {
    try { cp.execFileSync(cmd[0], [cmd[1]], { stdio: "ignore" }); return true; } catch {}
  }
  return false;
}

function listenWithFallback(srv, port, tries, cb) {
  srv.once("error", e => {
    if (e.code === "EADDRINUSE" && tries > 0) {
      console.error("porta " + port + " ocupada — tentando " + (port + 1));
      listenWithFallback(srv, port + 1, tries - 1, cb);
    } else { throw e; }
  });
  srv.listen(port, "127.0.0.1", () => cb(port));
}

module.exports = { buildSnapshot, createServer, DEFAULT_PORT };

if (require.main === module) {
  const pi = process.argv.indexOf("--port");
  const port = pi >= 0 ? Number(process.argv[pi + 1]) : DEFAULT_PORT;
  const srv = createServer({});
  listenWithFallback(srv, port, 10, p => {
    const url = "http://127.0.0.1:" + p + "/";
    console.log("ctx-dash em " + url + "  (Ctrl+C p/ sair)");
    openBrowser(url);
  });
}
```

- [ ] **Step 4: Rodar e ver passar**

Run: `node --test claude/test/`
Expected: PASS — 14 testes.

- [ ] **Step 5: Verificar com `curl` contra dados reais**

```bash
node claude/ctx-dash.js --port 4791 &
sleep 1
curl -s http://127.0.0.1:4791/api/sessions | node -e 'const d=JSON.parse(require("fs").readFileSync(0)); console.log("sessões:",d.sessions.length,"vivas:",d.sessions.filter(s=>s.live).length,"jobs:",d.jobs.length); console.log(d.sessions[0])'
curl -s -o /dev/null -w '%{http_code}\n' -X POST http://127.0.0.1:4791/api/sessions   # 405
kill %1
```

Expected: JSON com as sessões reais; POST retorna `405`.

- [ ] **Step 6: Confirmar que não escuta fora do loopback**

Run: `node claude/ctx-dash.js --port 4791 & sleep 1; ss -ltnp | grep 4791; kill %1`
Expected: endereço local `127.0.0.1:4791`, **nunca** `0.0.0.0:4791`.

- [ ] **Step 7: Commit**

```bash
git add claude/ctx-dash.js claude/test/server.test.js
git commit -m "feat(ctx-dash): servidor HTTP em 127.0.0.1 com SSE e API somente-leitura

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

### Task 7: Página — grid de cards ao vivo

**Files:**
- Create: `claude/ctx-dash.html`

**Interfaces:**
- Consumes: `GET /api/sessions`, `GET /api/events`.
- Produces: `<div id="grid">` com um `<article class="card">` por sessão.

- [ ] **Step 1: Escrever a página**

Estrutura obrigatória (CSS/JS inline, sem CDN, tema claro e escuro via `prefers-color-scheme`):

- `<header>`: contagem de sessões vivas, jobs ativos, relógio, toggle "mostrar inativas".
- `#grid`: cards.

Cada card contém, nesta hierarquia visual:

1. **Topo:** título (`title` ou `id.slice(0,8)`), `●` verde se `live`; à direita, badges de `permissionMode`, `speed` e `bg` (se houver job com aquele `sessionId`).
2. **Sub-linha:** `where` · `branch` · `model` · `v{version}`.
3. **Contexto (dominante):** barra com largura `pctFull`%, cor por faixa (`<50` verde `#2f9e44`, `50–80` âmbar `#e8a33d`, `≥80` vermelho `#e03131`); texto `50,000 / 200k (25%)`; **sparkline SVG** de `sparkline[]`; tendência (`↑ 1.2k tok/min · cheio em ~19 min`, ou `→ estável`).
4. **Rodapé (grade 2×3):** turnos · cache hit % · custo (`—` se `cost === null`) · subagents · compacts · web reqs.

Badge de `permissionMode` — **cores exatas do spec**:

```js
const MODE_STYLE = {
  bypassPermissions: { bg: "#e03131", fg: "#fff" },   // único perigoso
  plan:              { bg: "#5f3dc4", fg: "#fff" },   // read-only, deliberando
  acceptEdits:       { bg: "#e8a33d", fg: "#000" },
  default:           { bg: "#868e96", fg: "#fff" },
  auto:              null                              // linha de base: sem badge
};
```

Sparkline (SVG à mão, sem lib):

```js
function sparkline(values, w = 160, h = 28) {
  if (!values || values.length < 2) return "";
  const max = Math.max(...values) || 1;
  const pts = values.map((v, i) =>
    `${(i / (values.length - 1) * w).toFixed(1)},${(h - v / max * h).toFixed(1)}`).join(" ");
  return `<svg viewBox="0 0 ${w} ${h}" width="${w}" height="${h}" aria-hidden="true">
    <polyline fill="none" stroke="currentColor" stroke-width="1.5" points="${pts}"/></svg>`;
}
```

Atualização ao vivo:

```js
let showAll = false;
async function refresh() {
  const r = await fetch("/api/sessions");
  const d = await r.json();
  render(d);
}
new EventSource("/api/events").addEventListener("change", refresh);
setInterval(refresh, 5000);   // idades e detecção de processo entrando/saindo
refresh();
```

Escapar todo texto vindo do transcript (títulos, branch) antes de injetar:

```js
const esc = s => String(s == null ? "" : s).replace(/[&<>"']/g,
  c => ({ "&": "&amp;", "<": "&lt;", ">": "&gt;", '"': "&quot;", "'": "&#39;" }[c]));
```

- [ ] **Step 2: Abrir contra as sessões reais**

```bash
node claude/ctx-dash.js
```

Expected: browser abre; os cards batem com `bash claude/ctx-watch.sh --all` (mesmos ids, mesmos %); sessões vivas primeiro com `●` verde; nenhuma sessão em `auto` mostra badge.

- [ ] **Step 3: Verificar reatividade do SSE**

Com o dashboard aberto, mandar uma mensagem em outra sessão Claude.
Expected: o card daquela sessão atualiza `ctx`/`%` em menos de 1 segundo, sem refresh manual.

- [ ] **Step 4: Verificar tema escuro**

Alternar o tema do SO (ou DevTools → Rendering → `prefers-color-scheme: dark`).
Expected: fundo escuro, texto legível, cores das barras preservadas.

- [ ] **Step 5: Commit**

```bash
git add claude/ctx-dash.html
git commit -m "feat(ctx-dash): grid de cards ao vivo com sparkline, tendência e badges

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

### Task 8: Drill-down por sessão

**Files:**
- Modify: `claude/ctx-dash.html`

**Interfaces:**
- Consumes: `GET /api/session/:id/timeline` → `{points, metrics, subagents, workflows}`.
- Produces: painel de detalhe (dialog) aberto ao clicar num card.

- [ ] **Step 1: Implementar o painel**

Ao clicar num card, `fetch("/api/session/" + id + "/timeline")` e abrir um `<dialog>` com:

1. **Contexto × tempo** — gráfico de linha SVG. Anotar cada ponto com `compact === true` como marcador vertical tracejado e rótulo `compact` (queda de contexto). Eixo X: tempo; eixo Y: tokens até `metrics.window`.
2. **Custo acumulado × tempo** — se `metrics.byModel` render `costOf` não-nulo; senão, mostrar tokens acumulados e a nota `preço não verificado para este modelo`.
3. **Ferramentas** — barras horizontais de `metrics.tools` (nome + contagem), ordenadas desc.
4. **Subagents** — tabela: `agentType`, `description`, `model`, tokens in/out.
5. **Transições de modo** — lista de `metrics.modeTransitions` (`at`, `from` → `to`).
6. **Workflows** — se `workflows.length === 0`, mostrar `nenhum workflow registrado`. **Não** desenhar UI de detalhe até validar o formato com um workflow real.

Escala do gráfico (sem lib). `t0`/`t1` são o primeiro e o último timestamp; `maxY` é `metrics.window`:

```js
function linePath(points, w, h, maxY, t0, t1) {
  if (points.length < 2 || !maxY) return "";
  const x = t => (t1 === t0 ? 0 : (t - t0) / (t1 - t0) * w);
  const y = v => h - (v / maxY) * h;
  return points
    .map((p, i) => `${i ? "L" : "M"}${x(p.ts).toFixed(1)},${y(p.ctx).toFixed(1)}`)
    .join(" ");
}
```

- [ ] **Step 2: Verificar contra uma sessão real com compact**

Encontrar uma sessão que já sofreu compact:

```bash
grep -l compactMetadata ~/.claude/projects/*/*.jsonl | head -1
```

Abrir o dashboard, clicar no card dessa sessão.
Expected: o gráfico mostra o penhasco e o marcador `compact` no ponto certo.

Se nenhuma sessão tiver compact, registrar isso no commit e validar só o caminho sem compact.

- [ ] **Step 3: Verificar subagents**

Clicar no card da sessão que rodou o agente `claude-code-guide`.
Expected: tabela lista `claude-code-guide` com modelo `haiku-4-5-20251001` e tokens > 0.

- [ ] **Step 4: Commit**

```bash
git add claude/ctx-dash.html
git commit -m "feat(ctx-dash): drill-down com contexto×tempo, compacts, ferramentas e subagents

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

### Task 9: `install.sh`, alias e documentação

**Files:**
- Modify: `install.sh`
- Modify: `README.md`
- Modify: `claude/ctx-watch.sh` (documentar `--web` no cabeçalho)

**Interfaces:**
- Consumes: todos os arquivos criados.
- Produces: instalação idempotente + alias `ctxd`.

- [ ] **Step 1: Atualizar `install.sh`**

Após a linha `cp "$REPO_DIR/claude/ctx-watch.sh" ...`, acrescentar:

```bash
cp "$REPO_DIR/claude/ctx-lib.js"   "$CLAUDE_DIR/ctx-lib.js"
cp "$REPO_DIR/claude/ctx-watch.js" "$CLAUDE_DIR/ctx-watch.js"
cp "$REPO_DIR/claude/ctx-dash.js"  "$CLAUDE_DIR/ctx-dash.js"
cp "$REPO_DIR/claude/ctx-dash.html" "$CLAUDE_DIR/ctx-dash.html"
```

E, no bloco do alias, acrescentar o `ctxd` (mesmo padrão idempotente do `ctx`):

```bash
if grep -q "alias ctxd=" "$ZSHRC"; then
  echo "  alias ctxd já existe — pulando."
else
  printf "alias ctxd='node ~/.claude/ctx-dash.js'\n" >> "$ZSHRC"
  echo "  alias ctxd adicionado."
fi
```

- [ ] **Step 2: Rodar o instalador e verificar idempotência**

```bash
./install.sh && ./install.sh && ls -1 ~/.claude/ctx-*.{js,sh,html} && grep -c "alias ctxd=" ~/.zshrc
```

Expected: dois runs sem erro; os 5 arquivos presentes; `grep -c` retorna exatamente `1`.

- [ ] **Step 3: Verificar o dashboard instalado**

```bash
node ~/.claude/ctx-dash.js --port 4799 &
sleep 1 && curl -s -o /dev/null -w '%{http_code}\n' http://127.0.0.1:4799/
kill %1
```

Expected: `200`.

- [ ] **Step 4: Documentar no `README.md`**

Acrescentar seção descrevendo: `ctx` (terminal), `ctxd` (dashboard), bind em `127.0.0.1`, somente leitura, e o link para o spec.

- [ ] **Step 5: Rodar a suíte inteira**

Run: `node --test claude/test/`
Expected: PASS — todos os testes, 0 falhas.

- [ ] **Step 6: Commit**

```bash
git add install.sh README.md claude/ctx-watch.sh
git commit -m "feat(ctx-dash): instalação, alias ctxd e documentação

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Verificação end-to-end (após a Task 9)

1. `node --test claude/test/` → todos passam.
2. `bash claude/ctx-watch.sh --all` → saída idêntica à de antes do refactor (idades normalizadas).
3. `ctxd` → dashboard abre; cards batem com o terminal; sessões vivas primeiro.
4. Mandar mensagem em outra sessão → card atualiza em <1s (SSE).
5. `ss -ltnp | grep 4791` → apenas `127.0.0.1`.
6. `curl -X POST .../api/sessions` → `405`.
7. Clicar num card → drill-down com timeline, ferramentas e subagents reais.

## Fora de escopo (não implementar)

- Qualquer escrita/controle de sessão (modelo, esforço) — sem superfície suportada.
- Sink do Stream Deck (fase 2) — o motor de eventos do SSE já deixa o gancho pronto.
- Daemon/serviço em background; exposição na LAN.
- UI de detalhe de workflows — só depois de validar o formato do journal com um workflow real.
